# Storage & Recovery Lot Program

Desktop application for managing vehicle storage and recovery contracts. Launch with:

```
python lot_gui.py
```

The app supports intake, fee templates, contract tracking, and lien timeline reminders. Data is stored locally in `lot_data.json` alongside the application.
